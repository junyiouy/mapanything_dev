dataset_str:
  "WaymoToyWai(
    split='${dataset.waymo_toy_wai.train.split}',
    resolution=${dataset.waymo_toy_wai.train.dataset_resolution},
    principal_point_centered=${dataset.waymo_toy_wai.train.principal_point_centered},
    aug_crop=${dataset.waymo_toy_wai.train.aug_crop},
    transform='${dataset.waymo_toy_wai.train.transform}',
    data_norm_type='${dataset.waymo_toy_wai.train.data_norm_type}',
    ROOT='${dataset.waymo_toy_wai.train.ROOT}',
    dataset_metadata_dir='${dataset.waymo_toy_wai.train.dataset_metadata_dir}',
    overfit_num_sets=${dataset.waymo_toy_wai.train.overfit_num_sets},
    variable_num_views=${dataset.waymo_toy_wai.train.variable_num_views},
    num_views=${dataset.waymo_toy_wai.train.num_views},
    covisibility_thres=${dataset.waymo_toy_wai.train.covisibility_thres})"
split: 'train'
dataset_resolution: ${dataset.resolution_train}
principal_point_centered: ${dataset.principal_point_centered}
aug_crop: 16
transform: 'colorjitter+grayscale+gaublur'
data_norm_type: ${model.data_norm_type}
ROOT: ${root_data_dir}/waymo_train_toy
dataset_metadata_dir: "/wekafs/ict/junyiouy/map-anything/map_anything_dataset/map-anything/mapanything_dataset_metadata/train/waymo_scene_list_train_toy.npy"
overfit_num_sets: null
variable_num_views: ${dataset.train.variable_num_views}
num_views: ${dataset.num_views}
covisibility_thres: 0.25

dataset_str:
  "WaymoWAI(
    split='${dataset.waymo_wai.val.split}',
    resolution=${dataset.waymo_wai.val.dataset_resolution},
    principal_point_centered=${dataset.waymo_wai.val.principal_point_centered},
    aug_crop=${dataset.waymo_wai.val.aug_crop},
    transform='${dataset.waymo_wai.val.transform}',
    data_norm_type='${dataset.waymo_wai.val.data_norm_type}',
    ROOT='${dataset.waymo_wai.val.ROOT}',
    dataset_metadata_dir='${dataset.waymo_wai.val.dataset_metadata_dir}',
    overfit_num_sets=${dataset.waymo_wai.val.overfit_num_sets},
    variable_num_views=${dataset.waymo_wai.val.variable_num_views},
    num_views=${dataset.waymo_wai.val.num_views},
    covisibility_thres=${dataset.waymo_wai.val.covisibility_thres})"
split: 'val'
dataset_resolution: ${dataset.resolution_val_waymo}
principal_point_centered: ${dataset.principal_point_centered}
aug_crop: 0
transform: 'imgnorm'
data_norm_type: ${model.data_norm_type}
ROOT: ${root_data_dir}/waymo_val
dataset_metadata_dir: ${mapanything_dataset_metadata_dir}
overfit_num_sets: null
variable_num_views: ${dataset.val.variable_num_views}
num_views: ${dataset.num_views}
covisibility_thres: 0.25
